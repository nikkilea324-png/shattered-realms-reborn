name: CI Dispatcher Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  runner-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify runner
        run: |
          echo "GitHub runner started"
          uname -a
          git --version
